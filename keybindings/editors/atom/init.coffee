atom.commands.add 'atom-text-editor',
  'custom:insert-a': (event) ->
    editor = @getModel()
    editor.insertText('ā')

atom.commands.add 'atom-text-editor',
  'custom:insert-i': (event) ->
    editor = @getModel()
    editor.insertText('ī')

atom.commands.add 'atom-text-editor',
  'custom:insert-u': (event) ->
    editor = @getModel()
    editor.insertText('ū')

atom.commands.add 'atom-text-editor',
  'custom:insert-r': (event) ->
    editor = @getModel()
    editor.insertText('ṛ')

atom.commands.add 'atom-text-editor',
  'custom:insert-ri': (event) ->
    editor = @getModel()
    editor.insertText('ṝ')

atom.commands.add 'atom-text-editor',
  'custom:insert-l': (event) ->
    editor = @getModel()
    editor.insertText('ḷ')

atom.commands.add 'atom-text-editor',
  'custom:insert-li': (event) ->
    editor = @getModel()
    editor.insertText('ḹ')

atom.commands.add 'atom-text-editor',
  'custom:insert-m': (event) ->
    editor = @getModel()
    editor.insertText('ṃ')

atom.commands.add 'atom-text-editor',
  'custom:insert-malt': (event) ->
    editor = @getModel()
    editor.insertText('ṁ')

atom.commands.add 'atom-text-editor',
  'custom:insert-h': (event) ->
    editor = @getModel()
    editor.insertText('ḥ')

atom.commands.add 'atom-text-editor',
  'custom:insert-t': (event) ->
    editor = @getModel()
    editor.insertText('ṭ')

atom.commands.add 'atom-text-editor',
  'custom:insert-d': (event) ->
    editor = @getModel()
    editor.insertText('ḍ')

atom.commands.add 'atom-text-editor',
  'custom:insert-n': (event) ->
    editor = @getModel()
    editor.insertText('ṇ')

atom.commands.add 'atom-text-editor',
  'custom:insert-nalt': (event) ->
    editor = @getModel()
    editor.insertText('ṅ')

atom.commands.add 'atom-text-editor',
  'custom:insert-ntilde': (event) ->
    editor = @getModel()
    editor.insertText('ñ')

atom.commands.add 'atom-text-editor',
  'custom:insert-s': (event) ->
    editor = @getModel()
    editor.insertText('ś')

atom.commands.add 'atom-text-editor',
  'custom:insert-sh': (event) ->
    editor = @getModel()
    editor.insertText('ṣ')

atom.commands.add 'atom-text-editor',
  'custom:insert-dots': (event) ->
    editor = @getModel()
    editor.insertText('…')

atom.commands.add 'atom-text-editor',
  'custom:insert-emdash': (event) ->
    editor = @getModel()
    editor.insertText('—')

atom.commands.add 'atom-text-editor',
  'custom:insert-A': (event) ->
    editor = @getModel()
    editor.insertText('Ā')

atom.commands.add 'atom-text-editor',
  'custom:insert-I': (event) ->
    editor = @getModel()
    editor.insertText('Ī')

atom.commands.add 'atom-text-editor',
  'custom:insert-U': (event) ->
    editor = @getModel()
    editor.insertText('Ū')

atom.commands.add 'atom-text-editor',
  'custom:insert-R': (event) ->
    editor = @getModel()
    editor.insertText('Ṛ')

atom.commands.add 'atom-text-editor',
  'custom:insert-Ri': (event) ->
    editor = @getModel()
    editor.insertText('Ṝ')

atom.commands.add 'atom-text-editor',
  'custom:insert-L': (event) ->
    editor = @getModel()
    editor.insertText('Ḷ')

atom.commands.add 'atom-text-editor',
  'custom:insert-Li': (event) ->
    editor = @getModel()
    editor.insertText('Ḹ')

atom.commands.add 'atom-text-editor',
  'custom:insert-M': (event) ->
    editor = @getModel()
    editor.insertText('Ṃ')

atom.commands.add 'atom-text-editor',
  'custom:insert-Malt': (event) ->
    editor = @getModel()
    editor.insertText('Ṁ')

atom.commands.add 'atom-text-editor',
  'custom:insert-H': (event) ->
    editor = @getModel()
    editor.insertText('Ḥ')

atom.commands.add 'atom-text-editor',
  'custom:insert-T': (event) ->
    editor = @getModel()
    editor.insertText('Ṭ')

atom.commands.add 'atom-text-editor',
  'custom:insert-D': (event) ->
    editor = @getModel()
    editor.insertText('Ḍ')

atom.commands.add 'atom-text-editor',
  'custom:insert-N': (event) ->
    editor = @getModel()
    editor.insertText('Ṇ')

atom.commands.add 'atom-text-editor',
  'custom:insert-Nalt': (event) ->
    editor = @getModel()
    editor.insertText('Ṅ')

atom.commands.add 'atom-text-editor',
  'custom:insert-Ntilde': (event) ->
    editor = @getModel()
    editor.insertText('Ñ')

atom.commands.add 'atom-text-editor',
  'custom:insert-S': (event) ->
    editor = @getModel()
    editor.insertText('Ś')

atom.commands.add 'atom-text-editor',
  'custom:insert-Sh': (event) ->
    editor = @getModel()
    editor.insertText('Ṣ')

atom.commands.add 'atom-text-editor',
  'custom:insert-mcandrabindu': (event) ->
    editor = @getModel()
    editor.insertText('m̐')

atom.commands.add 'atom-text-editor',
  'custom:insert-Mcandrabindu': (event) ->
    editor = @getModel()
    editor.insertText('M̐')
